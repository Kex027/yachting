import { Box } from "@mui/joy";

const Divider = () => {
  return (
    <Box
      sx={{
        borderTop: "1px solid white",
        width: "100%",
        fontSize: { lg: "1.25rem" },
      }}
    ></Box>
  );
};

export default Divider;
